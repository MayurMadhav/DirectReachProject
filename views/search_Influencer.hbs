<!doctype html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

    <title>Search for Influencer</title>
  </head>
  <body>
    <div class="container">
      <h1 class="text-center text-primary mt-3 mb-3">Search for Influencer</h1>

      <form class="form-inline my-2 my-lg-0" method="get" action="/search">
        <input class="form-control mr-sm-2" type="search" placeholder="Search by First Name or Area of Interest" aria-label="Search" name="search_query" id="search_query">
      </form>

      <div class="mt-3">
        {{#if results}}
          <table class="table">
            <thead>
              <tr>
                <th scope="col">First Name</th>
                <th scope="col">AreaOfInterest</th>
              </tr>
            </thead>
            <tbody id="table-body">
              {{#each results}}
                <tr>
                  <td>{{this.FirstName}}</td>
                  <td>{{this.AreaOfInterest}}</td>
                </tr>
              {{/each}}
            </tbody>
          </table>
        {{else}}
          <p>No results found.</p>
        {{/if}}
      </div>

    </div>

    <script>
      const searchBox = document.getElementById("search_query");
      const tableBody = document.getElementById("table-body");
      const tableRows = tableBody.getElementsByTagName("tr");

      searchBox.addEventListener("keyup", function() {
        const searchText = searchBox.value.toLowerCase();
        for (let i = 0; i < tableRows.length; i++) {
          const rowText = tableRows[i].getElementsByTagName("td")[0].innerText.toLowerCase();
          if (rowText.includes(searchText)) {
            tableRows[i].style.display = "";
          } else {
            tableRows[i].style.display = "none";
          }
        }
      });
    </script>

  </body>
</html>


{{!-- 
correct


<!doctype html>
<html lang="en">
    <head>
        <!-- Required meta tags -->
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
            <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

    <title>Search for Influencer</title>
</head>
<body>
    <div class="container">
        <h1 class="text-center text-primary mt-3 mb-3">Search for Influencer</h1>
                    
        <form class="form-inline my-2 my-lg-0" method="get" action="/search">
            <input class="form-control mr-sm-2" type="search" placeholder="Search by First Name" aria-label="Search" name="search_query" id="search_query">
        </form>

        <div class="mt-3">
            {{#if results}}
                <table class="table">
                    <thead>
                        <tr>
                            <th scope="col">First Name</th>
                        </tr>
                    </thead>
                    <tbody id="table-body">
                        {{#each results}}
                            <tr>
                                <td>{{this.FirstName}}</td>
                            </tr>
                        {{/each}}
                    </tbody>
                </table>
            {{else}}
                <p>No results found.</p>
            {{/if}}
        </div>

    </div>

    <script>
        const searchBox = document.getElementById("search_query");
        const tableBody = document.getElementById("table-body");
        const tableRows = tableBody.getElementsByTagName("tr");

        searchBox.addEventListener("keyup", function() {
            const searchText = searchBox.value.toLowerCase();
            for (let i = 0; i < tableRows.length; i++) {
                const rowText = tableRows[i].getElementsByTagName("td")[0].innerText.toLowerCase();
                if (rowText.includes(searchText)) {
                    tableRows[i].style.display = "";
                } else {
                    tableRows[i].style.display = "none";
                }
            }
        });
    </script>

</body>
</html> --}}

























{{!--
<!doctype html>
<html lang="en">

<head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

    <title>Search for Influencer</title>
</head>

<body>
    <div class="container">
        <h1 class="text-center text-primary mt-3 mb-3">Search for Influencer</h1>

        <form class="form-inline my-2 my-lg-0" method="get" action="/search">
            <input class="form-control mr-sm-2" type="search" placeholder="Search by First Name" aria-label="Search"
                name="search_query">
            <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
        </form>

        <div class="mt-3">
            {{#if results}}
            <table class="table">
                <thead>
                    <tr>
                        <th scope="col">First Name</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each results}}
                    <tr>
                        <td>{{this.FirstName}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
            {{else}}
            <p>No results found.</p>
            {{/if}}
        </div>

    </div>
</body>

</html> --}}